import 'package:flutter/material.dart';
 
class RegistrarTareo extends StatelessWidget {
  const RegistrarTareo({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
       body: Padding(
        padding: const EdgeInsets.all(20.0),
        child: SingleChildScrollView(
          child: Column(
            children: <Widget>[
               
            ],
          ),
        ),
      ),
    );
  }
}